import React, { PureComponent } from "react";
import * as Constants from "../../Constants";

class VoteInProgress extends PureComponent{
  render(){
    return (
      <div>vote in progress</div>
    );
  }
}

export default VoteInProgress;